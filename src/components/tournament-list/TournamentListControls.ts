import styled from 'styled-components';

const TournamentListControls = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: space-between;
`;

export default TournamentListControls;
